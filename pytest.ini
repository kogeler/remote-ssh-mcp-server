[pytest]
addopts = -n auto --dist=worksteal --cov --cov-report=term --cov-report=xml
	-m 'not host'
pythonpath =
	.
testpaths = tests
asyncio_mode = auto
markers =
	host: needs the host itself or a real launcher and is excluded by default
